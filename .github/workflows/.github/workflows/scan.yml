name: TrendSentry Scanner
on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger scan
        run: curl -s -X POST "https://app.base44.com/api/scanTrigger" -H "Content-Type: application/json" -d "{}"
